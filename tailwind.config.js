/** @type {import('tailwindcss').Config} */
export default {
    content: [
      "./index.html",       // Root HTML file
      "./src/**/*.{js,css}" // All JS and CSS files inside src/
    ],
    darkMode: "class",       // Enables class-based dark mode
    theme: {
      extend: {},
    },
    plugins: [],
}
  